void main() {
  // testWidgets('Market Page UI Widget Test', (WidgetTester tester) async {
  //   // Build our app and trigger a frame.
  //   await tester.pumpWidget(MultiProvider(
  //     providers: [
  //       ChangeNotifierProvider(create: (context) => ApplicationState()),
  //     ],
  //     child: const MaterialApp(
  //       home: Scaffold(
  //         body: MarketPage(),
  //       ),
  //     ),
  //   ));

  //   // Verify if the page title exists
  //   expect(find.text('Market Place'), findsOneWidget);

  //   // Verify if DiscourseAppBar exists
  //   expect(find.byType(DiscourseAppBar), findsOneWidget);

  //   // Verify if SearchModule exists
  //   expect(find.byType(SearchModule), findsOneWidget);

  //   // Verify if FloatingActionButton exists
  //   expect(find.byType(FloatingActionButton), findsOneWidget);

  //   // Verify if ListingCard exists, there are two test items on the page
  //   expect(find.byType(ListingCard), findsNWidgets(2));

  //   // Verify if the icon of the FloatingActionButton is correct
  //   expect(
  //       find.widgetWithIcon(FloatingActionButton, Icons.add), findsOneWidget);
  // });
}
